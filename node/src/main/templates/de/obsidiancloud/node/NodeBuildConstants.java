package de.obsidiancloud.node;

/** This class contains all generated constants. */
public class NodeBuildConstants {
    /** The version of ObsidianCloud. */
    public static final String VERSION = "${version}";

    /** The name of the paper platform plugin/file. */
    public static final String PAPER_PLATFORM_FILE = "${paper_platform_file}";

    /** The name of the velocity platform plugin/file. */
    public static final String VELOCITY_PLATFORM_FILE = "${velocity_platform_file}";
}
